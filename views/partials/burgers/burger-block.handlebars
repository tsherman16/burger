<li>
	{{burger_name}}

	<button class="change-devoured btn btn-secondary btn-sm" data-id="{{id}}" data-newDevoured="{{devoured}}">
		{{#if devoured}}Devour!{{else}}Replace!{{/if}}
	</button>

    <button class="delete-burger btn btn-danger btn-sm" data-id="{{this.id}}">DELETE!</button>
</li>